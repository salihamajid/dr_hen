services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: drhen
      POSTGRES_PASSWORD: drhen
      POSTGRES_DB: drhen
    ports:
      - "5432:5432"
    volumes:
      - drhen-db-data:/var/lib/postgresql/data

volumes:
  drhen-db-data:
